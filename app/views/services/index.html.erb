<p id="notice"><%= notice %></p>

<h1>サービス内容一覧</h1>

<table>
  <thead>
    <tr>
      <th>Service_name</th>
      <th>price</th>
      <th>content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @services.each do |service| %>
      <tr>
        <td><%= service.service_name %>
        <td><%= service.price %>
        <td><%= service.content %>
        <% if @service.present? %>
        <td><%= link_to '詳細', factory_service_path(@service.factory_id,@service) %></td>
        <% end %>
        <td><%= link_to '編集', edit_factory_service_path(@factory, service) %></td>
        <td><%= link_to '削除', factory_service_path(@factory, service), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'サービス内容新規登録', new_factory_service_path %>
