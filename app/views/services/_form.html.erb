 <%= form_with(model: [@factory, @service], url: @service.new_record? ? factory_services_url(@factory, factory_id: @factory.id) : factory_service_url(@factory, @service, factory_id: @factory.id), local: true, method: @service.new_record? ? :post : :patch) do |form| %>
  <% if @service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
        <% @service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :factory_id, value: @factory.id %>

  <div class="form-group">
    <%= form.label :サービス名 %>
    <%= form.text_field :service_name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :金額 %>
    <%= form.number_field :price, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :内容等 %>
    <%= form.text_field :content, class: "form-control" %>
  </div>

  <div class="form-actions">
    <%= form.submit "登録", class: "btn btn-primary" %>
  </div>
<% end %>