<h1>登録ユーザーのページ</h1>
  <% if @user.image.present? %>
		<%= image_tag @user.image.url,class: 'rounded-circle mr15', width: '150', height: '150' %>
	<% else %>
		<%= image_tag 'default.jpg',width: '150', height: '150' %>
	<% end %>
<p>名前 : <%= @user.name %></p>
<p>メールアドレス : <%= @user.email %></p>
<p>プロフィール : <%= @user.profile %></p>

<p><td class="users-show-edit">お気に入りの工場</td></p>
			<% if @favorite_list.present? %>
				<% @favorite_list.each do |factory| %>
					<p><%= factory.factory_name %>:
					<%= link_to "この工場のページへ行く", factory_path(factory) %></p>
				<% end %>
			<% end %>

<%= link_to "プロフィール編集", edit_user_registration_path(current_user.id) %>
<%= link_to "ホーム",root_path %>